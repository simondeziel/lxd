# Pinned Trivy image for vulnerability scanning.
# This is the single source of truth for the image reference; action.yml reads from here at runtime.
# Dependabot (docker ecosystem) will keep the digest up to date.
# Add a version tag (e.g. aquasec/trivy:0.69.3@sha256:...) to allow Dependabot to also track version bumps.
FROM aquasec/trivy@sha256:d8c558c4e69b59664afb2bfa99009ec30538f99e46a7beab7df2bbdfbc3ed58a
